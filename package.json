{
  "name": "bar-utopia",
  "private": true,
  "version": "2.0.0",
  "//": "Thin wrapper. The Next.js app lives in frontend/; server code and the DB schema live in backend/. Dependencies are declared in frontend/package.json and hoisted here by npm workspaces, so backend/ can resolve them too.",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "start": "npm run start -w frontend",
    "typecheck": "npm run typecheck -w frontend"
  }
}
